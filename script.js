const loader=document.getElementById('loader');window.addEventListener('load',()=>setTimeout(()=>loader.style.opacity=0,700));setTimeout(()=>{loader.style.visibility='hidden'},1600);
const menu=document.querySelector('.menu'),mm=document.querySelector('.mobile-menu');menu?.addEventListener('click',()=>mm.classList.toggle('open'));mm?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mm.classList.remove('open')));
const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>obs.observe(x));
const canvas=document.getElementById('particles'),ctx=canvas.getContext('2d');let W,H,d=[];function size(){W=canvas.width=innerWidth*devicePixelRatio;H=canvas.height=innerHeight*devicePixelRatio;d=Array.from({length:Math.min(110,Math.floor(innerWidth/10))},()=>({x:Math.random()*W,y:Math.random()*H,r:Math.random()*1.5+.2,v:Math.random()*.35+.05,a:Math.random()*.5+.1}))}function draw(){ctx.clearRect(0,0,W,H);ctx.fillStyle='#b6f000';d.forEach(p=>{p.y-=p.v;if(p.y<0)p.y=H;ctx.globalAlpha=p.a;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,7);ctx.fill()});requestAnimationFrame(draw)}size();draw();addEventListener('resize',size);
addEventListener('mousemove',e=>{const c=document.querySelector('.cursor');if(c){c.style.left=e.clientX+'px';c.style.top=e.clientY+'px'}});
document.querySelectorAll('.skill-card,.project-card,.score,.timeline-card').forEach(el=>{el.addEventListener('mousemove',e=>{if(innerWidth<901)return;let r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(800px) rotateX(${y*-5}deg) rotateY(${x*5}deg) translateY(-5px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});
addEventListener('scroll',()=>{document.documentElement.style.setProperty('--scroll',scrollY/(document.documentElement.scrollHeight-innerHeight)*100+'%')});
