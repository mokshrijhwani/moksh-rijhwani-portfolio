const loader=document.getElementById('loader');window.addEventListener('load',()=>{setTimeout(()=>{loader.style.opacity='0';loader.style.visibility='hidden'},1700)});
const menu=document.querySelector('.menu'),mobile=document.querySelector('.mobile-menu');menu?.addEventListener('click',()=>{mobile.classList.toggle('open');menu.textContent=mobile.classList.contains('open')?'×':'☰'});mobile?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobile.classList.remove('open');menu.textContent='☰'}));
const reveal=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>reveal.observe(el));
const sections=[...document.querySelectorAll('main section[id]')],links=[...document.querySelectorAll('.nav nav a')];const activeObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){links.forEach(l=>l.classList.toggle('active',l.getAttribute('href')==='#'+e.target.id))}}),{rootMargin:'-35% 0px -55% 0px'});sections.forEach(s=>activeObserver.observe(s));
const canvas=document.getElementById('particles'),ctx=canvas.getContext('2d');let W=0,H=0,particles=[];function resize(){const dpr=Math.min(devicePixelRatio||1,2);W=innerWidth;H=innerHeight;canvas.width=W*dpr;canvas.height=H*dpr;canvas.style.width=W+'px';canvas.style.height=H+'px';ctx.setTransform(dpr,0,0,dpr,0,0);particles=Array.from({length:Math.min(75,Math.max(30,Math.floor(W/18)))},()=>({x:Math.random()*W,y:Math.random()*H,r:.4+Math.random()*1.3,v:.08+Math.random()*.3,a:.1+Math.random()*.35}))}function tick(){ctx.clearRect(0,0,W,H);ctx.fillStyle='#b8f000';for(const p of particles){p.y-=p.v;if(p.y<0)p.y=H;ctx.globalAlpha=p.a;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(tick)}resize();tick();addEventListener('resize',resize);
addEventListener('mousemove',e=>{const c=document.querySelector('.cursor');if(c){c.style.left=e.clientX+'px';c.style.top=e.clientY+'px'}});
if(innerWidth>900){document.querySelectorAll('.project,.tool,.achievement,.job').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(700px) rotateX(${y*-3}deg) rotateY(${x*3}deg) translateY(-3px)`});el.addEventListener('mouseleave',()=>el.style.transform='')})}
let last=0;addEventListener('scroll',()=>{const y=scrollY;document.body.style.setProperty('--scroll',y/(document.documentElement.scrollHeight-innerHeight)*100+'%');if(Math.abs(y-last)>40){last=y;document.querySelector('.nav')?.classList.toggle('scrolled',y>20)}});
